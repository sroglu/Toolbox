# Changelog

## [1.0.0] - 2026-07-16
- Initial release: `ViewManager` (persistent page views with a back-stack + on-demand instance views), the `IView` / `ViewComponent` managed-view contract, and the pluggable `IViewInstantiator` (`DefaultViewInstantiator`, or a Spawner-backed pooling adapter).
